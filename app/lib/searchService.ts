export interface SearchResult{type:'scan'|'report'|'guide'|'tool';title:string;description:string;url:string;score?:number}
export function searchContent(query:string):SearchResult[]{const q=query.toLowerCase();const results:SearchResult[]=[];if(q.includes('scan')||q.includes('kiem tra'))results.push({type:'tool',title:'Kiem tra URL',description:'Kiem tra do an toan cua website',url:'/scan'});if(q.includes('report')||q.includes('bao cao'))results.push({type:'tool',title:'Bao cao lua dao',description:'Bao cao website dang ngo',url:'/report'});if(q.includes('quiz')||q.includes('trac nghiem'))results.push({type:'tool',title:'Trac nghiem',description:'Kiem tra kien thuc chong lua dao',url:'/quiz'});return results}feat: add global search service
